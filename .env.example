# Copy to .env and fill in locally. Never commit .env (CLAUDE.md §8).
# Keys only — no real values in this file.

NODE_ENV=development

# Database (added in T2 / T4)
DATABASE_URL=

# Redis (added in T2)
REDIS_URL=
