{
  "name": "@pospay/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared kernel — Money, KWD rounding, Percentage, TaxRule. Pure TypeScript, zero dependencies, runs in Node and the browser",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@pospay/config": "workspace:*",
    "eslint": "10.11.0",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
