{
  "name": "@pospay/observability",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Logging configuration shared by api and worker — pino options and the redaction list (CLAUDE.md §8)",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@pospay/config": "workspace:*",
    "@types/node": "24.13.6",
    "eslint": "10.11.0",
    "typescript": "6.0.3",
    "vitest": "5.0.1"
  }
}
